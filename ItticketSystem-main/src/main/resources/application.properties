# Application name
spring.application.name=itticketsys

# Server port (default is 8080; uncomment to change)
# server.port=8080

# MySQL Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/itticketdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=karthi@0903

# JPA & Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Optional CORS config via properties (not required if using @CrossOrigin)
# You can configure allowed origins in code instead.
